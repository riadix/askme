<div class="container">

  <h2 class="title-lg mb">Question <%= @question.id %></h2>
  <p class="mt-sm">
    <%= link_to "All questions for @#{@question.user.nickname}", user_path(@question.user) %>
  </p>

</div>

<div class="container mt-sm">
  <%= render 'question', question: @question %>
</div>
